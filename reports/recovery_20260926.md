# 2026-09-26: B/16 four-GPU progress and recovery

Slurm job 101979 ran on four H100 80 GB GPUs from 01:01:57 to 11:04:58 EDT. It completed and checkpointed 40 epochs (50,040 optimizer updates), then failed during epoch 41. The checkpoint retains the model, optimizer, three EMA copies, and each rank's random states. L/16 has not started.

The last five complete epochs averaged 14.6 minutes each. At that measured rate, the remaining 160 training epochs require about 39 hours, excluding evaluation, repeated work after interruptions, and queue delays. The 40-epoch monitoring result is FID-8K 25.1137 at CFG 2.9 and EMA 0.9996; it is not a final FID-50K result. Raw evidence is in [b16_4gpu_monitor_ep040.json](b16_4gpu_monitor_ep040.json) and [training.csv](training.csv).

The failure log contains `LLVM ERROR: pthread_join failed: Invalid argument`, followed by rank 1 reporting a DataLoader worker killed by SIGABRT. Training used the Linux default fork start method after CUDA/Inductor initialization. This is consistent with inherited thread-state problems described by [PyTorch's multiprocessing guidance](https://docs.pytorch.org/docs/2.14/notes/multiprocessing.html); the precise LLVM failure has not been independently reproduced.

The runner now explicitly spawns fresh training DataLoader processes and uses a picklable crop callable. Workers still restart at epoch boundaries to preserve the existing generator-based seed and checkpoint semantics. Model code, training hyperparameters, effective batch 1024, learning rate 2e-4, and CFG 2.9 are unchanged.

The real-ImageNet CPU regression passed: loading after Inductor compilation, unchanged preprocessing, and exact next-epoch tensor/label agreement after restoring the saved loader generator state. See [dataloader_spawn_validation.json](dataloader_spawn_validation.json). This validates the loader change on CPU, not the resumed full GPU training.

Dependent job 101980 is queued and will resume from the 40-epoch checkpoint, starting epoch 41. GPU recovery remains unverified until that job starts. The main report now reads the four-GPU run directories and the current B/16, L/16 scope.

## Resume observed at 13:44 EDT

Job 101980 started at 13:38:42 EDT on alphagpu07 with four H100s. Its log confirms checkpoint restoration at 13:39:25 (40 completed epochs and 50,040 optimizer updates), followed by epoch 41 steps 121 and 321. GPU training has resumed with the loader change; extended stability and the next completed epoch are still being monitored.
